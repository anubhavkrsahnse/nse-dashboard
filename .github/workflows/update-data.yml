name: Update market data

on:
  schedule:
    # Mon-Fri, after exchange EOD files publish:
    # 22:00 IST (16:30 UTC) primary + 00:30 IST next-day (19:00 UTC) safety net
    - cron: "30 16 * * 1-5"
    - cron: "0 19 * * 1-5"
  workflow_dispatch:
    inputs:
      backfill_days:
        description: "Backfill this many calendar days (0 = normal daily run)"
        required: false
        default: "0"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install -r pipeline/requirements.txt

      - name: Fetch data
        run: |
          DAYS="${{ github.event.inputs.backfill_days || '0' }}"
          if [ "$DAYS" != "0" ]; then
            python pipeline/update.py --backfill "$DAYS"
          else
            python pipeline/update.py
          fi

      - name: Commit updated data
        run: |
          git config user.name "data-bot"
          git config user.email "actions@github.com"
          git add site/data
          git diff --cached --quiet || git commit -m "data: $(date -u +%F)"
          git push
